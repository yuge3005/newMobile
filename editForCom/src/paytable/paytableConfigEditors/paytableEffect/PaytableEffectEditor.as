package paytable.paytableConfigEditors.paytableEffect{
	import fl.controls.Button;
	
	import settings.EditorItem;
	
	public class PaytableEffectEditor extends EditorItem{
		
		private var closeBtn: Button;
		private var loadBtn: Button;
		private var saveBtn: Button;
		private var deleteBtn: Button;
		
		public function PaytableEffectEditor(){
			
		}
	}
}